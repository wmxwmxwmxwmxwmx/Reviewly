# BFF proxy target (Python Gateway)
API_URL=http://127.0.0.1:3001

# Set to 1 to log API requests/responses in the browser console (dev only)
NEXT_PUBLIC_DEBUG_API=0

# Set to 1 for local UI without GitHub OAuth (never in production)
NEXT_PUBLIC_PRISM_AUTH_BYPASS=0
